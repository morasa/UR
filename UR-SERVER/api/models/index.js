'use strict';
import {user} from './users';
import {menu} from './menus';
import {role} from './roles';

const model = {};
model.user = user;
model.menu = menu;
model.role = role;

module.exports = model;
